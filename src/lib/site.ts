// TODO: swap this for the real domain once one is bought — for now the
// live site is served from the default Vercel URL.
export const SITE_URL = process.env.NEXT_PUBLIC_SITE_URL || 'https://dlbc-youth-choir.vercel.app'
export const SITE_NAME = 'DLBC Youth Choir Makurdi'
