NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Set once a real domain is bought — used for canonical URLs, sitemap,
# and social share links. Defaults to the Vercel URL if unset.
NEXT_PUBLIC_SITE_URL=
